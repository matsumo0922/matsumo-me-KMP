# Compose Multiplatform Application

### Wasm Browser (Alpha)
Run the browser application: `./gradlew :frontend:wasmJsBrowserDevelopmentRun --continue`
Run browser UI tests: `./gradlew :frontend:wasmJsBrowserTest`
